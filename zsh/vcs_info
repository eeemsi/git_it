# Enable vcs_info
setopt PROMPT_SUBST

# Specify the vcs (:vcs_info:vcs-string:user-context:repo-root-name)
zstyle ':vcs_info:git:' enable

# Specify how it should be formatted
zstyle ':vcs_info:*' formats '%b'

precmd() {
    vcs_info
}

