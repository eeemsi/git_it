# Enable vcs_info
setopt PROMPT_SUBST

# Specify the vcs
zstyle ':vcs_info:*' enable git

# Specify how it should be formatted
zstyle ':vcs_info:*' formats "- %F{cyan}%s:%b%f"

precmd() {
    vcs_info
}
